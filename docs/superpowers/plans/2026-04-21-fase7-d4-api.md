# API Pública con Autenticación por API Key — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Exponer un subconjunto de datos de SIPEEM como API REST pública (datos de municipios, historial electoral, resultados de proyección) protegida por API keys, para consumo por sistemas externos de la campaña.

**Architecture:** Tabla `api_keys` en Supabase con hash bcrypt de la key. Middleware que valida el header `X-API-Key` para rutas `/api/v1/*`. Los endpoints retornan JSON puro sin autenticación de sesión. Las keys se gestionan desde el panel de admin.

**Tech Stack:** Next.js 16 Route Handlers · Supabase · `bcryptjs` (ya disponible o instalar) · Next.js Middleware

---

## Context

- Proyecto en `M:/SIPPEEM/sipeem/`
- NO hay git — NO hacer commits
- Leer `src/middleware.ts` para entender el sistema actual de protección
- Los endpoints `/api/v1/*` NO usan sesión de usuario — usan header `X-API-Key`
- Solo exponer datos de lectura (GET) — nunca mutaciones por API pública

---

### Task 1: Tabla `api_keys` en Supabase

- [ ] **Step 1: Ejecutar en Supabase SQL Editor**

```sql
CREATE TABLE public.api_keys (
  id              bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  nombre          text NOT NULL,                    -- Label descriptivo (ej: "Sistema de medios")
  key_hash        text NOT NULL UNIQUE,             -- SHA-256 hash de la key real
  key_prefix      text NOT NULL,                    -- Primeros 8 chars para identificar (ej: "sk_sipee")
  activa          boolean NOT NULL DEFAULT true,
  creada_por      text NOT NULL,                    -- email del admin que la creó
  ultimo_uso      timestamptz,
  usos_totales    bigint NOT NULL DEFAULT 0,
  created_at      timestamptz NOT NULL DEFAULT now()
);

-- Solo admins y directores pueden ver las keys
ALTER TABLE public.api_keys ENABLE ROW LEVEL SECURITY;
CREATE POLICY "admin_access_api_keys" ON public.api_keys
  FOR ALL TO authenticated
  USING (true);  -- El control real está en las Server Actions (rol check)
```

Nota: Usamos SHA-256 (via `crypto.subtle` en Node.js) en lugar de bcrypt para evitar dependencia extra. El hash es suficientemente seguro para API keys generadas aleatoriamente.

---

### Task 2: Utilidad de API keys

**Files:**
- Create: `src/lib/api-keys.ts`

- [ ] **Step 1: Crear el módulo**

```typescript
// src/lib/api-keys.ts
import { createServiceClient } from "@/lib/supabase/service";

/**
 * Genera una API key aleatoria y su hash SHA-256.
 * La key real se muestra UNA VEZ al crear — el hash se guarda en DB.
 */
export function generarApiKey(): { key: string; prefix: string } {
  const bytes = crypto.getRandomValues(new Uint8Array(32));
  const key = "sk_sipeem_" + Array.from(bytes)
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
  const prefix = key.slice(0, 18); // "sk_sipeem_" + 8 hex chars
  return { key, prefix };
}

/**
 * Hashea una API key con SHA-256 usando Web Crypto API (disponible en Node.js 18+).
 */
export async function hashApiKey(key: string): Promise<string> {
  const encoder = new TextEncoder();
  const data = encoder.encode(key);
  const hashBuffer = await crypto.subtle.digest("SHA-256", data);
  const hashArray = Array.from(new Uint8Array(hashBuffer));
  return hashArray.map((b) => b.toString(16).padStart(2, "0")).join("");
}

/**
 * Valida una API key contra la DB. Retorna true si es válida y activa.
 * También actualiza `ultimo_uso` y `usos_totales` en background.
 */
export async function validarApiKey(key: string): Promise<boolean> {
  if (!key || !key.startsWith("sk_sipeem_")) return false;

  try {
    const hash = await hashApiKey(key);
    const svc = createServiceClient();

    const { data, error } = await svc
      .from("api_keys")
      .select("id, activa")
      .eq("key_hash", hash)
      .single();

    if (error || !data || !data.activa) return false;

    // Update usage stats fire-and-forget
    svc
      .from("api_keys")
      .update({ ultimo_uso: new Date().toISOString(), usos_totales: data.usos_totales + 1 })
      .eq("id", data.id)
      .then(() => {});

    return true;
  } catch {
    return false;
  }
}
```

---

### Task 3: Server Actions para gestión de keys

**Files:**
- Create: `src/actions/api-keys.ts`

- [ ] **Step 1: Crear el archivo**

```typescript
// src/actions/api-keys.ts
"use server";

import { revalidatePath } from "next/cache";
import { createServiceClient } from "@/lib/supabase/service";
import { getUsuarioActual } from "./auth";
import { redirect } from "next/navigation";
import { generarApiKey, hashApiKey } from "@/lib/api-keys";

export type ApiKey = {
  id: number;
  nombre: string;
  key_prefix: string;
  activa: boolean;
  creada_por: string;
  ultimo_uso: string | null;
  usos_totales: number;
  created_at: string;
};

export type NuevaKeyResult = {
  key: string;       // La key completa — mostrar UNA SOLA VEZ
  registro: ApiKey;
};

export async function getApiKeys(): Promise<ApiKey[]> {
  const usuario = await getUsuarioActual();
  if (!usuario || !["director", "admin"].includes(usuario.rol)) redirect("/login");

  const svc = createServiceClient();
  const { data, error } = await svc
    .from("api_keys")
    .select("id, nombre, key_prefix, activa, creada_por, ultimo_uso, usos_totales, created_at")
    .order("created_at", { ascending: false });

  if (error) throw new Error(error.message);
  return (data ?? []) as ApiKey[];
}

export async function crearApiKey(nombre: string): Promise<NuevaKeyResult> {
  const usuario = await getUsuarioActual();
  if (!usuario || !["director", "admin"].includes(usuario.rol)) redirect("/login");

  if (!nombre.trim()) throw new Error("El nombre es requerido");

  const svc = createServiceClient();
  const { key, prefix } = generarApiKey();
  const hash = await hashApiKey(key);

  const { data, error } = await svc
    .from("api_keys")
    .insert({
      nombre: nombre.trim(),
      key_hash: hash,
      key_prefix: prefix,
      creada_por: usuario.email,
    })
    .select("id, nombre, key_prefix, activa, creada_por, ultimo_uso, usos_totales, created_at")
    .single();

  if (error) throw new Error(error.message);
  revalidatePath("/admin/api-keys");
  return { key, registro: data as ApiKey };
}

export async function revocarApiKey(id: number): Promise<void> {
  const usuario = await getUsuarioActual();
  if (!usuario || !["director", "admin"].includes(usuario.rol)) redirect("/login");

  const svc = createServiceClient();
  const { error } = await svc
    .from("api_keys")
    .update({ activa: false })
    .eq("id", id);

  if (error) throw new Error(error.message);
  revalidatePath("/admin/api-keys");
}
```

---

### Task 4: Actualizar middleware para rutas `/api/v1/*`

**Files:**
- Modify: `src/middleware.ts`

- [ ] **Step 1: Leer el middleware actual**

Leer `src/middleware.ts` completo.

- [ ] **Step 2: Excluir `/api/v1/*` del middleware de sesión**

Las rutas `/api/v1/*` NO deben pasar por la validación de sesión de usuario — tienen su propia autenticación por header. Agregar exclusión en el matcher o en la lógica del middleware:

```typescript
// En el matcher config, las rutas /api/v1/* deben estar excluidas
// O en la lógica: if (pathname.startsWith('/api/v1/')) return NextResponse.next();
```

La validación de API key ocurre DENTRO del route handler, no en middleware, para evitar crear un cliente Supabase en el edge (incompatible con `createServiceClient`).

---

### Task 5: Endpoints de la API pública

**Files:**
- Create: `src/app/api/v1/municipios/route.ts`
- Create: `src/app/api/v1/municipios/[id]/route.ts`
- Create: `src/app/api/v1/historial/route.ts`

- [ ] **Step 1: Helper para autenticación en route handlers**

Crear inline en cada route handler (no archivo separado para evitar complejidad):

```typescript
import { validarApiKey } from "@/lib/api-keys";
import { NextRequest, NextResponse } from "next/server";

async function autenticar(req: NextRequest): Promise<NextResponse | null> {
  const apiKey = req.headers.get("x-api-key") ?? req.headers.get("authorization")?.replace("Bearer ", "");
  if (!apiKey) {
    return NextResponse.json({ error: "API key requerida. Usa el header X-API-Key." }, { status: 401 });
  }
  const valida = await validarApiKey(apiKey);
  if (!valida) {
    return NextResponse.json({ error: "API key inválida o revocada." }, { status: 403 });
  }
  return null; // OK
}
```

- [ ] **Step 2: Endpoint `/api/v1/municipios`**

```typescript
// src/app/api/v1/municipios/route.ts
import { NextRequest, NextResponse } from "next/server";
import { validarApiKey } from "@/lib/api-keys";
import { createServiceClient } from "@/lib/supabase/service";

export async function GET(req: NextRequest) {
  const apiKey = req.headers.get("x-api-key") ?? req.headers.get("authorization")?.replace("Bearer ", "");
  if (!apiKey || !(await validarApiKey(apiKey))) {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 });
  }

  const svc = createServiceClient();
  const { data, error } = await svc
    .from("municipios")
    .select("id, nombre, color, prioridad, riesgo, estatus, responsable")
    .eq("estatus", "activo")
    .order("nombre");

  if (error) return NextResponse.json({ error: error.message }, { status: 500 });

  return NextResponse.json({
    data,
    total: data?.length ?? 0,
    generado_at: new Date().toISOString(),
  });
}
```

- [ ] **Step 3: Endpoint `/api/v1/municipios/[id]`**

```typescript
// src/app/api/v1/municipios/[id]/route.ts
import { NextRequest, NextResponse } from "next/server";
import { validarApiKey } from "@/lib/api-keys";
import { createServiceClient } from "@/lib/supabase/service";

export async function GET(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  const apiKey = req.headers.get("x-api-key") ?? req.headers.get("authorization")?.replace("Bearer ", "");
  if (!apiKey || !(await validarApiKey(apiKey))) {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 });
  }

  const { id } = await params;
  const municipioId = parseInt(id, 10);
  if (isNaN(municipioId)) {
    return NextResponse.json({ error: "ID inválido" }, { status: 400 });
  }

  const svc = createServiceClient();
  const [munRes, termRes, aspRes] = await Promise.all([
    svc.from("municipios").select("*").eq("id", municipioId).single(),
    svc.from("termometros").select("term1, term2, term3, term4, term5").eq("municipio_id", municipioId).maybeSingle(),
    svc.from("aspirantes").select("nombre, cargo_aspirado, partido, nivel_amenaza").eq("municipio_id", municipioId).eq("activo", true),
  ]);

  if (munRes.error || !munRes.data) {
    return NextResponse.json({ error: "Municipio no encontrado" }, { status: 404 });
  }

  const t = termRes.data;
  const avgTermometro = t
    ? Math.round((t.term1 + t.term2 + t.term3 + t.term4 + t.term5) / 5)
    : null;

  return NextResponse.json({
    data: {
      ...munRes.data,
      termometros: termRes.data ?? null,
      avg_termometro: avgTermometro,
      aspirantes: aspRes.data ?? [],
    },
    generado_at: new Date().toISOString(),
  });
}
```

- [ ] **Step 4: Endpoint `/api/v1/historial`**

```typescript
// src/app/api/v1/historial/route.ts
import { NextRequest, NextResponse } from "next/server";
import { validarApiKey } from "@/lib/api-keys";
import { createServiceClient } from "@/lib/supabase/service";

export async function GET(req: NextRequest) {
  const apiKey = req.headers.get("x-api-key") ?? req.headers.get("authorization")?.replace("Bearer ", "");
  if (!apiKey || !(await validarApiKey(apiKey))) {
    return NextResponse.json({ error: "No autorizado" }, { status: 401 });
  }

  const { searchParams } = new URL(req.url);
  const municipioId = searchParams.get("municipio_id");
  const anio = searchParams.get("anio");

  const svc = createServiceClient();
  let query = svc
    .from("historial_electoral")
    .select("municipio_id, anio, porcentaje_ganador, partido_ganador, participacion, municipios(nombre)")
    .order("anio", { ascending: false })
    .limit(500);

  if (municipioId) query = query.eq("municipio_id", parseInt(municipioId, 10));
  if (anio) query = query.eq("anio", parseInt(anio, 10));

  const { data, error } = await query;
  if (error) return NextResponse.json({ error: error.message }, { status: 500 });

  return NextResponse.json({
    data,
    total: data?.length ?? 0,
    filtros: { municipio_id: municipioId, anio },
    generado_at: new Date().toISOString(),
  });
}
```

---

### Task 6: Panel de gestión de API keys

**Files:**
- Create: `src/app/(protected)/admin/api-keys/page.tsx`

- [ ] **Step 1: Crear la página**

```tsx
// src/app/(protected)/admin/api-keys/page.tsx
import { getApiKeys } from "@/actions/api-keys";
import ApiKeysManager from "@/components/settings/ApiKeysManager";

export default async function ApiKeysPage() {
  const keys = await getApiKeys();
  return (
    <div className="p-6 max-w-4xl mx-auto space-y-6">
      <div>
        <h1 className="text-2xl font-black text-slate-900">API Keys</h1>
        <p className="text-sm text-slate-500 mt-1">
          Gestiona las claves de acceso para la API pública de SIPEEM.
        </p>
      </div>
      <ApiKeysManager initialKeys={keys} />
    </div>
  );
}
```

- [ ] **Step 2: Crear `ApiKeysManager` Client Component**

```tsx
// src/components/settings/ApiKeysManager.tsx
"use client";

import { useState } from "react";
import { crearApiKey, revocarApiKey, type ApiKey } from "@/actions/api-keys";
import { toast } from "sonner";
import { Copy, Plus, Trash2, CheckCircle, XCircle } from "lucide-react";

export default function ApiKeysManager({ initialKeys }: { initialKeys: ApiKey[] }) {
  const [keys, setKeys] = useState(initialKeys);
  const [nombre, setNombre] = useState("");
  const [creando, setCreando] = useState(false);
  const [nuevaKey, setNuevaKey] = useState<string | null>(null); // Mostrar UNA VEZ
  const [revocando, setRevocando] = useState<number | null>(null);

  const handleCrear = async () => {
    if (!nombre.trim()) return;
    setCreando(true);
    try {
      const { key, registro } = await crearApiKey(nombre);
      setKeys([registro, ...keys]);
      setNuevaKey(key);
      setNombre("");
      toast.success("API key creada. Copia la key ahora — no se mostrará de nuevo.");
    } catch (err) {
      toast.error(err instanceof Error ? err.message : "Error al crear key");
    } finally {
      setCreando(false);
    }
  };

  const handleRevocar = async (id: number) => {
    setRevocando(id);
    try {
      await revocarApiKey(id);
      setKeys(keys.map((k) => k.id === id ? { ...k, activa: false } : k));
      toast.success("API key revocada");
    } catch (err) {
      toast.error(err instanceof Error ? err.message : "Error al revocar");
    } finally {
      setRevocando(null);
    }
  };

  return (
    <div className="space-y-6">
      {/* Nueva key — alerta de una sola vez */}
      {nuevaKey && (
        <div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-4 space-y-2">
          <p className="text-sm font-bold text-emerald-800">
            API Key creada. Copia ahora — no se mostrará nuevamente.
          </p>
          <div className="flex items-center gap-2">
            <code className="flex-1 text-xs font-mono bg-white border border-emerald-200 rounded-lg px-3 py-2 text-emerald-900 break-all">
              {nuevaKey}
            </code>
            <button
              onClick={() => { navigator.clipboard.writeText(nuevaKey); toast.success("Copiado"); }}
              className="p-2 rounded-lg bg-emerald-100 hover:bg-emerald-200 text-emerald-700"
            >
              <Copy className="w-4 h-4" />
            </button>
          </div>
          <button
            onClick={() => setNuevaKey(null)}
            className="text-xs text-emerald-600 hover:text-emerald-800 underline"
          >
            Ya copié la key, cerrar
          </button>
        </div>
      )}

      {/* Formulario de creación */}
      <div className="rounded-2xl border border-slate-200 bg-white p-4 flex gap-3">
        <input
          type="text"
          value={nombre}
          onChange={(e) => setNombre(e.target.value)}
          placeholder="Nombre descriptivo (ej: Sistema de medios)"
          className="flex-1 rounded-xl border border-slate-200 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
        />
        <button
          onClick={handleCrear}
          disabled={creando || !nombre.trim()}
          className="flex items-center gap-2 px-4 py-2 rounded-xl bg-indigo-600 text-white text-sm font-bold disabled:opacity-50"
        >
          <Plus className="w-4 h-4" />
          {creando ? "Creando..." : "Crear key"}
        </button>
      </div>

      {/* Lista de keys */}
      <div className="space-y-2">
        {keys.length === 0 && (
          <p className="text-sm text-slate-400 text-center py-8">No hay API keys creadas.</p>
        )}
        {keys.map((k) => (
          <div key={k.id} className="rounded-2xl border border-slate-200 bg-white p-4 flex items-center gap-4">
            <div className="flex-1 min-w-0">
              <div className="flex items-center gap-2 mb-1">
                {k.activa
                  ? <CheckCircle className="w-4 h-4 text-emerald-500 flex-shrink-0" />
                  : <XCircle className="w-4 h-4 text-slate-300 flex-shrink-0" />
                }
                <span className="font-semibold text-slate-900 text-sm">{k.nombre}</span>
                {!k.activa && (
                  <span className="text-[9px] font-black uppercase tracking-widest text-slate-400 bg-slate-100 px-2 py-0.5 rounded-full">
                    Revocada
                  </span>
                )}
              </div>
              <div className="flex items-center gap-4 text-xs text-slate-400">
                <span>Prefijo: <code className="font-mono text-slate-600">{k.key_prefix}…</code></span>
                <span>Usos: {k.usos_totales.toLocaleString("es-MX")}</span>
                {k.ultimo_uso && (
                  <span>Último uso: {new Date(k.ultimo_uso).toLocaleDateString("es-MX")}</span>
                )}
              </div>
            </div>
            {k.activa && (
              <button
                onClick={() => handleRevocar(k.id)}
                disabled={revocando === k.id}
                className="p-2 rounded-xl text-rose-400 hover:text-rose-600 hover:bg-rose-50 disabled:opacity-50"
                title="Revocar key"
              >
                <Trash2 className="w-4 h-4" />
              </button>
            )}
          </div>
        ))}
      </div>

      {/* Documentación rápida */}
      <div className="rounded-2xl border border-slate-100 bg-slate-50 p-4 space-y-3">
        <h3 className="text-xs font-black uppercase tracking-widest text-slate-500">Uso de la API</h3>
        <div className="space-y-2 text-xs font-mono text-slate-600">
          <p className="text-slate-400 font-sans">Incluye el header en cada petición:</p>
          <code className="block bg-white border border-slate-200 rounded-lg px-3 py-2">
            X-API-Key: sk_sipeem_...
          </code>
          <p className="text-slate-400 font-sans mt-2">Endpoints disponibles:</p>
          <code className="block bg-white border border-slate-200 rounded-lg px-3 py-2 space-y-1">
            GET /api/v1/municipios<br />
            GET /api/v1/municipios/[id]<br />
            GET /api/v1/historial?municipio_id=1&anio=2021
          </code>
        </div>
      </div>
    </div>
  );
}
```

---

### Task 7: Agregar enlace en navegación de admin

**Files:**
- Modify: `src/components/layout/Sidebar.tsx` (o donde estén los nav links de admin)

- [ ] **Step 1: Leer el sidebar actual**

Leer `src/components/layout/Sidebar.tsx` (o el componente de navegación equivalente).

- [ ] **Step 2: Agregar enlace "API Keys"**

En la sección de configuración/admin del sidebar, agregar:

```tsx
{ href: "/admin/api-keys", label: "API Keys", icon: Key, roles: ["director", "admin"] }
```

Importar `Key` de `lucide-react`.

- [ ] **Step 3: Verificar build**

```bash
cd M:/SIPPEEM/sipeem && npm run build 2>&1 | tail -20
```
