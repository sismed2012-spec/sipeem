# Proyección Electoral con ML — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Mejorar la proyección electoral de B4 (que usa fórmula fija) con un modelo de regresión logística entrenado con el historial electoral del Edomex + variables de estructura territorial y termómetros. El modelo se ejecuta en el servidor con librería ligera de JS.

**Architecture:** Librería `ml-matrix` + regresión logística simple en Node.js (sin Python). El modelo se re-entrena en cada petición con los datos disponibles (sin persistencia del modelo — el dataset es pequeño). Los resultados reemplazan/complementan la proyección de B4.

**Tech Stack:** `ml-logistic-regression` o `simple-statistics` (npm) · Next.js Server Action · Supabase

---

## PREREQUISITO: B4 (Proyección electoral) completado

---

## Context

- Proyecto en `M:/SIPPEEM/sipeem/`
- NO hay git — NO hacer commits
- Dataset pequeño (~125 municipios × N años) — regresión logística simple es apropiada
- No usar Python/Flask/FastAPI — mantener todo en Next.js

---

### Task 1: Instalar librería de ML

- [ ] **Step 1: Instalar `simple-statistics`**

```bash
cd M:/SIPPEEM/sipeem && npm install simple-statistics
```

`simple-statistics` provee: media, desviación estándar, regresión lineal, correlación — suficiente para una proyección básica.

- [ ] **Step 2: Verificar instalación**

```bash
node -e "const ss = require('simple-statistics'); console.log(ss.mean([1,2,3]))"
```
Esperado: `2`

---

### Task 2: Server Action `getProyeccionML`

**Files:**
- Create: `src/actions/proyeccion-ml.ts`

- [ ] **Step 1: Crear el archivo**

```typescript
// src/actions/proyeccion-ml.ts
"use server";

import * as ss from "simple-statistics";
import { createServiceClient } from "@/lib/supabase/service";
import { getUsuarioActual } from "./auth";
import { redirect } from "next/navigation";

export type ProyeccionMLMunicipio = {
  municipio_id: number;
  nombre: string;
  // Regresion lineal: margen esperado basado en tendencia histórica
  margen_tendencia: number;   // votos de margen proyectados
  r_squared: number;          // calidad del ajuste (0-1)
  // Score combinado (de B4) + ajuste ML
  score_combinado: number;    // 0-100
  confianza: "baja" | "media" | "alta"; // basado en r_squared y cantidad de datos
};

export async function getProyeccionML(): Promise<ProyeccionMLMunicipio[]> {
  const usuario = await getUsuarioActual();
  if (!usuario || !["director", "admin"].includes(usuario.rol)) redirect("/login");

  const svc = createServiceClient();

  const [mRes, histRes, termRes] = await Promise.all([
    svc.from("municipios").select("id, nombre").eq("estatus", "activo"),
    svc.from("historial_electoral")
      .select("municipio_id, anio, porcentaje_ganador")
      .order("anio"),
    svc.from("termometros").select("municipio_id, term1, term2, term3, term4, term5"),
  ]);

  const termMap = new Map((termRes.data ?? []).map((t) => [t.municipio_id, t]));

  // Group history by municipio
  const histByMunicipio = new Map<number, { anio: number; pct: number }[]>();
  for (const row of histRes.data ?? []) {
    if (!histByMunicipio.has(row.municipio_id))
      histByMunicipio.set(row.municipio_id, []);
    histByMunicipio.get(row.municipio_id)!.push({ anio: row.anio, pct: row.porcentaje_ganador });
  }

  const results: ProyeccionMLMunicipio[] = (mRes.data ?? []).map((m) => {
    const hist = histByMunicipio.get(m.id) ?? [];
    const t = termMap.get(m.id);

    let margen_tendencia = 0;
    let r_squared = 0;
    let confianza: "baja" | "media" | "alta" = "baja";

    if (hist.length >= 3) {
      // Linear regression: year → percentage
      const points: [number, number][] = hist.map((h) => [h.anio, h.pct]);
      try {
        const regression = ss.linearRegression(points);
        const line = ss.linearRegressionLine(regression);

        // Project for next election (assume 3-year cycles, next = last + 3)
        const lastAnio = Math.max(...hist.map((h) => h.anio));
        const proyectedPct = line(lastAnio + 3);
        margen_tendencia = Math.max(0, Math.min(100, proyectedPct));

        // R-squared
        const actualValues = hist.map((h) => h.pct);
        const predictedValues = hist.map((h) => line(h.anio));
        r_squared = ss.rSquared(points, predictedValues.map((v, i) => [hist[i].anio, v]));
        r_squared = Math.max(0, Math.min(1, r_squared));

        confianza = hist.length >= 5 && r_squared > 0.6
          ? "alta"
          : hist.length >= 3 && r_squared > 0.3
          ? "media"
          : "baja";
      } catch {
        // Regression failed (e.g., all same year) — use mean
        margen_tendencia = ss.mean(hist.map((h) => h.pct));
      }
    } else if (hist.length > 0) {
      margen_tendencia = ss.mean(hist.map((h) => h.pct));
    }

    // Combine with termometros
    const avgTerm = t
      ? (t.term1 + t.term2 + t.term3 + t.term4 + t.term5) / 5
      : 50;

    // Weighted combination: 60% tendencia histórica + 40% termómetros
    const score_combinado = Math.round(
      margen_tendencia * 0.6 + avgTerm * 0.4
    );

    return {
      municipio_id: m.id,
      nombre: m.nombre,
      margen_tendencia: Math.round(margen_tendencia * 10) / 10,
      r_squared: Math.round(r_squared * 100) / 100,
      score_combinado: Math.min(100, Math.max(0, score_combinado)),
      confianza,
    };
  });

  return results.sort((a, b) => b.score_combinado - a.score_combinado);
}
```

---

### Task 3: Integrar en Sala de Situación

**Files:**
- Modify: `src/components/situacion/SituacionTable.tsx`
- Modify: `src/app/(protected)/admin/situacion/page.tsx`

- [ ] **Step 1: Agregar columna "Proyección ML" a la tabla**

En `situacion/page.tsx`, agregar `getProyeccionML()` al fetch y pasar los datos.
En `SituacionTable.tsx`, agregar columna con score + badge de confianza.

Colores de confianza:
- `alta` → verde
- `media` → ámbar
- `baja` → slate (grayed)

- [ ] **Step 2: Verificar build**

```bash
cd M:/SIPPEEM/sipeem && npm run build 2>&1 | tail -20
```
