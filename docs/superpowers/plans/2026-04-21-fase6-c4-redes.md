# Monitoreo de Redes Sociales (AI) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Módulo que permite buscar menciones de un municipio/candidato en la web, analizar el sentimiento con IA y guardar un resumen de "pulso digital" en la ficha municipal.

**Architecture:** Server Action `analizarPulsoDigital(municipioId)` que (1) hace búsqueda web vía la API de Tavily o Serper, (2) pasa los resultados a Claude para análisis de sentimiento, (3) guarda el resumen en tabla `pulso_digital`. Panel `PulsoDigitalPanel` en la ficha municipal con historial de análisis y botón "Analizar ahora".

**Tech Stack:** `ai` v6 · Vercel AI Gateway · Tavily API (búsqueda web) · Supabase

---

## PREREQUISITO: Fase 5 Setup completada + API Key de búsqueda web

Antes de ejecutar este plan, obtener una API key de:
- **Tavily** (recomendado): https://tavily.com — `TAVILY_API_KEY=tvly-...`
- O **Serper** (alternativa): https://serper.dev — `SERPER_API_KEY=...`

Agregar la key elegida a `.env.local`.

---

## Context

- Proyecto en `M:/SIPPEEM/sipeem/`
- NO hay git — NO hacer commits
- La búsqueda web es para información pública — no scraping de redes privadas
- Cada análisis cuesta tokens de IA + llamadas a API de búsqueda — diseñar para uso manual (no automático)

---

### Task 1: Tabla `pulso_digital` en Supabase

- [ ] **Step 1: Ejecutar en Supabase SQL Editor**

```sql
CREATE TABLE public.pulso_digital (
  id              bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  municipio_id    bigint NOT NULL REFERENCES public.municipios(id) ON DELETE CASCADE,
  query_usada     text NOT NULL,
  sentimiento     text CHECK (sentimiento IN ('muy_positivo','positivo','neutro','negativo','muy_negativo')),
  resumen         text NOT NULL,
  fuentes_count   int NOT NULL DEFAULT 0,
  generado_por    text NOT NULL,
  created_at      timestamptz NOT NULL DEFAULT now()
);
CREATE INDEX pulso_digital_municipio_idx ON public.pulso_digital (municipio_id);
```

---

### Task 2: Utilidad de búsqueda web

**Files:**
- Create: `src/lib/busqueda-web.ts`

- [ ] **Step 1: Crear el módulo**

```typescript
// src/lib/busqueda-web.ts

export type ResultadoBusqueda = {
  titulo: string;
  url: string;
  contenido: string;
};

/**
 * Busca información web sobre una query usando Tavily API.
 * Retorna hasta 5 resultados con título, URL y snippet.
 */
export async function buscarWeb(query: string): Promise<ResultadoBusqueda[]> {
  const apiKey = process.env.TAVILY_API_KEY;
  if (!apiKey) throw new Error("TAVILY_API_KEY no configurada en .env.local");

  const res = await fetch("https://api.tavily.com/search", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      api_key: apiKey,
      query,
      search_depth: "basic",
      max_results: 5,
      include_answer: false,
    }),
  });

  if (!res.ok) throw new Error(`Tavily API error: ${res.status}`);

  const data = await res.json() as {
    results: { title: string; url: string; content: string }[];
  };

  return (data.results ?? []).map((r) => ({
    titulo: r.title,
    url: r.url,
    contenido: r.content.slice(0, 500), // Limitar a 500 chars por resultado
  }));
}
```

---

### Task 3: Server Action `analizarPulsoDigital`

**Files:**
- Create: `src/actions/pulso-digital.ts`

- [ ] **Step 1: Crear el archivo**

```typescript
// src/actions/pulso-digital.ts
"use server";

import { revalidatePath } from "next/cache";
import { createServiceClient } from "@/lib/supabase/service";
import { getUsuarioActual } from "./auth";
import { redirect } from "next/navigation";
import { generateAnalysis } from "@/lib/ai";
import { buscarWeb } from "@/lib/busqueda-web";

export type PulsoDigital = {
  id: number;
  municipio_id: number;
  query_usada: string;
  sentimiento: string;
  resumen: string;
  fuentes_count: number;
  generado_por: string;
  created_at: string;
};

export async function getPulsoDigital(municipioId: number): Promise<PulsoDigital[]> {
  const usuario = await getUsuarioActual();
  if (!usuario || !["director", "admin"].includes(usuario.rol)) redirect("/login");

  const svc = createServiceClient();
  const { data, error } = await svc
    .from("pulso_digital")
    .select("*")
    .eq("municipio_id", municipioId)
    .order("created_at", { ascending: false })
    .limit(5);

  if (error) throw new Error(error.message);
  return (data ?? []) as PulsoDigital[];
}

export async function analizarPulsoDigital(
  municipioId: number,
  queryPersonalizada?: string
): Promise<PulsoDigital> {
  const usuario = await getUsuarioActual();
  if (!usuario || !["director", "admin"].includes(usuario.rol)) redirect("/login");

  const svc = createServiceClient();

  // Get municipality name
  const { data: mun } = await svc
    .from("municipios")
    .select("nombre")
    .eq("id", municipioId)
    .single();

  const nombre = mun?.nombre ?? `Municipio ${municipioId}`;
  const query = queryPersonalizada ?? `${nombre} Estado de México política elecciones candidato 2025 2026`;

  // 1. Search web
  const resultados = await buscarWeb(query);

  // 2. Analyze with AI
  const contexto = resultados
    .map((r, i) => `[${i + 1}] ${r.titulo}\n${r.contenido}`)
    .join("\n\n");

  const prompt = `Analiza las siguientes noticias y menciones web sobre el municipio de ${nombre}, Estado de México:

${contexto}

Genera un análisis de pulso digital con:
1. **Sentimiento general**: clasifica como: muy_positivo, positivo, neutro, negativo, o muy_negativo
2. **Resumen ejecutivo** (2-3 párrafos): ¿Qué se dice del municipio/candidato? ¿Qué temas dominan?
3. **Alertas** (si hay noticias negativas relevantes): mencionar brevemente

Formato de respuesta:
SENTIMIENTO: [clasificación]
---
[resumen y análisis]

Máximo 250 palabras. En español.`;

  const analisis = await generateAnalysis(prompt);

  // Parse sentimiento from response
  const sentimientoMatch = analisis.match(/SENTIMIENTO:\s*(\w+)/i);
  const sentimiento = sentimientoMatch?.[1]?.toLowerCase().replace(/\s+/g, "_") ?? "neutro";
  const resumen = analisis.replace(/SENTIMIENTO:.*\n---\n?/i, "").trim();

  // Save to DB
  const { data, error } = await svc
    .from("pulso_digital")
    .insert({
      municipio_id: municipioId,
      query_usada: query,
      sentimiento: ["muy_positivo","positivo","neutro","negativo","muy_negativo"].includes(sentimiento) ? sentimiento : "neutro",
      resumen,
      fuentes_count: resultados.length,
      generado_por: usuario.email,
    })
    .select("*")
    .single();

  if (error) throw new Error(error.message);
  revalidatePath(`/admin/estrategia-municipal/${municipioId}`);
  return data as PulsoDigital;
}
```

---

### Task 4: Componente `PulsoDigitalPanel`

**Files:**
- Create: `src/components/actores/PulsoDigitalPanel.tsx`

- [ ] **Step 1: Crear el panel**

Panel con:
- Historial de últimos 5 análisis (badge de sentimiento + fecha + resumen colapsable)
- Input opcional para query personalizada
- Botón "Analizar ahora" que llama a `analizarPulsoDigital`

Seguir el patrón de `TermometrosDiagnostico` para el botón de generación con loading state.

Colores de sentimiento:
```typescript
const SENTIMIENTO_COLORS = {
  muy_positivo: "bg-emerald-100 text-emerald-700 border-emerald-200",
  positivo: "bg-green-100 text-green-700 border-green-200",
  neutro: "bg-slate-100 text-slate-600 border-slate-200",
  negativo: "bg-orange-100 text-orange-700 border-orange-200",
  muy_negativo: "bg-red-100 text-red-700 border-red-200",
};
```

---

### Task 5: Integrar en `ActoresTabs`

- [ ] **Step 1: Agregar tab "Pulso Digital"**

1. Obtener `pulso` en `getActoresMunicipio` via `getPulsoDigital(municipioId).catch(() => [])`
2. Agregar `pulso: PulsoDigital[]` a `ActoresMunicipioData`
3. Agregar `<TabsTrigger value="pulso">Pulso Digital</TabsTrigger>`
4. `<TabsContent value="pulso"><PulsoDigitalPanel municipioId={municipioId} initialPulso={actores.pulso} /></TabsContent>`

- [ ] **Step 2: Verificar build**

```bash
cd M:/SIPPEEM/sipeem && npm run build 2>&1 | tail -20
```
